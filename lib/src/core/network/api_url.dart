class ApiUrl {
  static const baseUrl = "https://api.github.com";
  static const issues = "$baseUrl/search/issues";
  static const users = "$baseUrl/search/users";
  static const repositories = "$baseUrl/search/repositories";
}
